/*
 * Storage Engine configuration header.
 *
 * DO NOT EDIT: GENERATED BY CMAKE
 */

#ifndef __CONFIG_H__
#define __CONFIG_H__

struct config {
	char *log_file;
	char *err_log_file;
	char *db_file;
	int persist_level;
};

${HAVE_DBG}

${HAVE_LINUX}
${HAVE_RECURSION}

#define SQLITE_DB "${DATA_PATH}/xfire-dbg.db"

#define XFIRE_STACK_SIZE ${STACK_SIZE}

#define DEBUG_DB_NAME "dbg-db"
#ifdef HAVE_DBG
#define HAVE_DEBUG
#endif

#endif
