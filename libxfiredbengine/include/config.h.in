/*
 * Storage Engine configuration header.
 *
 * DO NOT EDIT: GENERATED BY CMAKE
 */

#ifndef __CONFIG_H__
#define __CONFIG_H__

/**
 * @brief Configuration structure.
 */
struct config {
	char *log_file; //!< stdout file.
	char *err_log_file; //!< stderr log file
	char *db_file; //!< SQLite database file.
	int persist_level; //!< Persistency level.
};

${HAVE_DBG}

${HAVE_RECURSION}

${HAVE_LINUX}
#ifdef HAVE_LINUX
#define HAVE_POSIX
#endif

#define SQLITE_DB "${DATA_PATH}/xfire-dbg.db"

#define XFIRE_STACK_SIZE ${STACK_SIZE}

#define DEBUG_DB_NAME "dbg-db"
#ifdef HAVE_DBG
#define HAVE_DEBUG
#endif

#endif
