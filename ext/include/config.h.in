/*
 * CMake config input
 */

#ifndef __CONFIG_H__
#define __CONFIG_H__

${HAVE_DBG}
${HAVE_LOG}

${XFIRE_STDERR} "${STDERR}"
${XFIRE_STDOUT} "${STDOUT}"
${HAVE_LINUX}
${HAVE_RECURSION}

#define XFIRE_STACK_SIZE ${STACK_SIZE}

#define SQLITE_DB "${DATA_PATH}/xfire.db"

#define DEBUG_DB_NAME "dbg-db"
#ifdef HAVE_DBG
#define HAVE_DEBUG
#endif

#endif
